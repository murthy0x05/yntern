export const metadata = {
  title: "Yntern® — Recruiter Registration",
  description: "Register as a recruiter on Yntern to start scouting talent.",
};

export default function RecruiterRegisterLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
