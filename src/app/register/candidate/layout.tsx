export const metadata = {
  title: "Yntern® — Candidate Registration",
  description: "Register as a candidate on Yntern to get discovered by top recruiters.",
};

export default function CandidateRegisterLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
